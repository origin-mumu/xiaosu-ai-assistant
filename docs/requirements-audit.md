# 笔试题目要求与交付功能逐项对照表

本对照表严格对照 `招聘笔试题.html` 中的每一项考核要求与加分项进行逐条核验，确保所有功能点均有真实代码与可验证链路支撑。

---

## 一、核心功能要求对照

| 模块 | 笔试题具体要求 | 实现状态 | 关键实现与验证代码位置 |
|---|---|---|---|
| **知识库管理** | 上传 md / pdf / word / txt 等格式 | **完全实现** | [`apps/api/src/xiaosu/knowledge/service.py`](file:///apps/api/src/xiaosu/knowledge/service.py)，支持 4 种主流格式解析 |
| **知识库管理** | 列表与索引处理状态展示 | **完全实现** | `pending ➡️ indexing ➡️ indexed / failed` 状态机流转与前端 Tag 标签展示 |
| **知识库管理** | 删除文档与切片同步清理 | **完全实现** | 数据库外键级联删除 `document_chunks` 向量，杜绝“幽灵切片” |
| **知识库管理** | 同名文件替换与防重复累加 | **完全实现** | SHA-256 校验和去重，内容变更自动替换旧版本切片与向量 |
| **智能问答** | 回答附带文件名/章节/页码/段落引用 | **完全实现** | `Citation` 结构化对象，回答末尾附带精准引用卡片与高亮直达链接 |
| **智能问答** | 多轮对话上下文记忆 | **完全实现** | 按 `(platform, tenant_id, conversation_id, user_id)` 严格隔离，支持 20 轮上下文记忆 |
| **智能问答** | 流式打字机逐字输出 | **完全实现** | 原生 SSE `delta` 流式传输，Vue 3 `reactive` + `nextTick` 毫秒级打字渲染 |
| **智能问答** | 低相关度严格拒答机制 | **完全实现** | 向量相似度低于 0.35 阈值时强制拒答，杜绝模型虚构信息 |
| **智能问答** | 点击引用弹出高亮原文抽屉 | **完全实现** | 前端 `DocumentPreviewPane.vue`，PDF 翻至对应页，文本/Word 高亮对应段落 |
| **工具调用** | 内部 Mock 业务 API 集成 | **完全实现** | 员工姓名查询、员工详情、考勤打卡记录、订单销售汇总 4 个业务工具 |
| **工具调用** | 通用时间工具 | **完全实现** | `get_current_time` 支持精准时区计算，供模型推算相对日期 |
| **工具调用** | 模型自主决策（严禁 if/else 路由） | **完全实现** | 原生 JSON Schema 工具注册，大模型自主进行 Tool Calling 决策 |
| **管理后台** | 系统概览仪表盘 | **完全实现** | 统计卡片、双供应商模型状态、知识库统计、最近对话与响应时间折线图 |
| **管理后台** | 对话日志全要素审计 | **完全实现** | 记录时间、用户、提问、答案、工具参数与结果、Token、预估费用、耗时及状态 |
| **管理后台** | 知识库与模型参数配置 | **完全实现** | 支持分块大小、重叠、Top K、相似度阈值以及多模型供应商持久化热切换 |
| **IM 集成** | 钉钉群聊 / 单聊接入 | **完全实现** | 采用钉钉官方最新的 Stream 长连接双向通信协议，**无需公网 IP 与内网穿透** |
| **部署交付** | Docker Compose 一键启动 | **完全实现** | `docker compose up --build` 一键拉起 API、Web、PostgreSQL (pgvector) 与 Bot |

---

## 二、加分项与工程规范对照

| 加分项 / 规范要求 | 题目标准 | 实际达成情况 |
|---|---|---|
| **多模型供应商适配** | 通过统一接口同时支持 ≥ 2 家供应商 | **已达成**：通过抽象适配层同时原生接入 **阿里百炼（通义千问）** 与 **智谱清言（GLM 系列）**，管理后台支持可视化一键热切换，底层 RAG 向量索引完美解耦兼容。 |
| **代码文件行数控制** | 单文件 ≤ 500 行（Python/TS/Vue） | **已达成**：全项目所有源代码文件均保持在 500 行以内。 |
| **目录文件数量控制** | 单目录 ≤ 8 个文件 | **已达成**：受控源码目录文件数均保持在 8 个以内，辅助脚本模块化拆分归档。 |
| **自动化测试覆盖** | 核心链路自动化验证 | **已达成**：23 项自动化测试（含离线 Mock LLM 状态机测试、多供应商工厂测试、时区计算测试），CI 构建与 TypeScript 检查 100% 通过。 |
| **安全红线保障** | 严禁泄漏真实 API Key | **已达成**：`.env` 严格纳入 `.gitignore`，版本库仅保留 `.env.example` 模板。 |

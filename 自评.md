# 项目自评

## 1. 你做的是什么、解决谁的什么问题？

我做的是一个**企业内部智能助手（小苏）**，通过「钉钉 IM」和「Web 管理后台」双端交付：
- **解决普通员工的痛点**：员工无需在各系统间来回翻找文档或打扰人事/财务，直接在钉钉群聊或私聊中以自然语言提问，即可精准查询规章制度（年假、报销等附带原文切片定位）、员工档案、考勤记录、销售订单汇总及时间推算。
- **解决企业管理员的痛点**：管理员无需技术背景，通过 Web 后台即可完成 PDF/Word/Markdown/TXT 知识库的上传、版本替换与切片管理，审计每一次问答的工具调用与 Token 成本，并能一键热切换底层大模型。

---

## 2. 你最得意的 1 个技术决策 + 1 个被迫的妥协？

### 🌟 最得意的 1 个技术决策：**LLM 与 Embedding 的架构解耦**
在支持「阿里百炼 Qwen ↔ 智谱清言 GLM-4」多模型供应商热切换时，我没有简单地将问答模型与向量模型捆绑切换，而是将**上层问答大脑（LLM）**与**底层检索索引（Embedding）**完全解耦。上层大模型可以在后台自由热切换，而底层检索向量始终与数据库中已建立索引的切片保持一致。这样既避免了跨厂商向量空间数学正交导致的检索失效（相似度极低、0 卡片），又无需在每次切换大模型时推倒重算几十万字的向量数据库，实现了真正的零成本无缝多模型热插拔。

### ⚖️ 1 个被迫的妥协：**业务系统采用本地 JSON 数据集而非真实企业 API**
根据笔试题要求和本地离线测试环境的限制，员工档案、考勤打卡和销售订单数据是在后端通过标准的本地 JSON 数据集与强类型 Service 进行模拟（`MockInternalSystemService`），没有直接对接真实的钉钉官方考勤 OpenAPI 或企业 ERP 系统。不过整个 Agent 工具调用的架构是完全生产化的，如果未来要在真实企业落地，只需替换 Service 内部的数据读取逻辑为第三方 HTTP/RPC 调用即可。

---

## 3. 如果再给你 1 周，你会优先做什么？

1. **补齐细粒度的业务与数据权限校验（RBAC）**：
   - 增加员工与工具之间的数据权限控制。例如：普通员工提问时只能查询自己的考勤记录，禁止跨人员越权查询他人隐私；部门主管只能汇总本部门考勤与订单数据；敏感的财务与薪酬文档切片增加角色可见范围隔离，防止低权限员工通过提问越权获取机密。
2. **打通企业级 SSO 单点登录与钉钉免登**：
   - 将 Web 管理后台的单一管理员账号密码登录，升级为对接企业 LDAP / 钉钉扫码与工作台免登授权，实现操作人身份与真实企业组织架构自动绑定。
3. **优化知识库异步处理与进度反馈**：
   - 针对几十 MB 的超大 PDF 或批量文档上传，引入后台 Celery/Redis 异步任务队列，在前端提供实时的解析切片进度条与错误行高亮，进一步提升大文件批量导入时的交互体验。

# 小苏企业智能助手自评

## 做的是什么

我实现了一套企业内部 AI 助手：员工在钉钉中用自然语言查询制度、员工、考勤、订单和时间；管理员在 Web 后台维护多格式知识库并审计回答、引用、工具、Token 和失败原因。它解决的是企业知识分散、内部系统查询入口不统一，以及 AI 回答难追溯的问题。

## 最得意的技术决策

最得意的是把 IM 适配与智能处理彻底分层。Web 和钉钉都进入同一个 `ChatService`，再由千问根据 Function Calling schema 自主选择知识检索或内部工具。会话使用“平台 + 企业 + 会话 + 用户”联合身份隔离；引用、Token、成本、错误和工具结果在同一条链路落库。这样新增 IM 渠道或新工具时，改动边界很清楚，也避免各端产生不同回答策略。

拒答不是只依赖 Prompt：如果模型调用知识检索但没有超过阈值的引用，系统层会覆盖任何生成内容，固定回复“文档里没找到”。这是为了把“不瞎编”从提示词愿望变成可测试约束。

## 一个被迫的妥协

管理后台目前是单管理员账号，模型切换也是进程内临时设置，重启后恢复 `.env`，且不会同步到独立的钉钉容器。这样避免把密钥或部署配置写回磁盘，适合笔试演示，但生产环境应接入公司 SSO/RBAC，把非敏感模型配置放入数据库或配置中心，并增加变更审计。

## 如果再给一周

第一优先级是接入公司 SSO 与细粒度 RBAC；第二是用 Alembic 管理生产迁移；第三是在钉钉使用可更新的 AI 卡片。之后接 OpenTelemetry/Langfuse，对检索、模型原生流和工具调用做完整链路分析，再用 Evals 结果调优切片长度、相似度阈值和提示词。
